
# (in your blog env, from project root)
# 1. Stop tracking .env and delete it from Git
git rm --cached .env

# 2. Add .env to .gitignore
echo ".env" >> .gitignore

# 3. Commit these changes
git add .gitignore
git commit -m "chore: remove .env from tracking and add to .gitignore"
